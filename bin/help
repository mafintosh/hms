Usage: hms [command] [remote] [service?] [options?]

Available commands are:
  completion   setup tab completion
  remotes      manage remotes
  add          add a new service
  update       update an existing service
  remove       remove and stop a service
  deploy       deploy cwd to your service
  tarball      fetch the service as a tgz file
  list         lists available services
  start        start service processes
  stop         stop service processes
  restart      restart service processes
  log          tail service logs
  sync         force a sync
  status       exit with code != 0 if service crashed

Use the following commands to setup a remote
  dock         start a service dock
  terminal     start a service terminal
